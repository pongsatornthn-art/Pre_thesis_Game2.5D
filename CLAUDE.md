# Pre_thesis_Game2.5D — อ่านก่อนเริ่มงานทุกครั้ง

เกม 2.5D สืบสวน/สยองขวัญ · โลก 3D + ตัวละครสไปรท์พิกเซล · กลไกหลัก = สลับโลกปกติ ↔ โลก PTSD
Unity 6000.0.33f1 · URP · Cinemachine · TextMeshPro

> ไฟล์นี้โหลดอัตโนมัติทุกแชทใหม่ — **ห้ามยาว** เก็บแค่กติกา + ชี้ทางว่าไปอ่านต่อที่ไหน

---

## ⚠️ กติกา (เจ้าของกำหนด — สำคัญที่สุด)

| งาน | ขออนุญาตก่อนไหม |
|---|---|
| แก้โค้ด `.cs` | **ไม่ต้อง** ทำเลย (พังแล้ว git ย้อนง่าย) |
| **แก้อะไรก็ตามใน Unity Editor** — ซีน / prefab / ค่าใน Inspector / เพิ่ม-ลบ component / Animator | **ต้องถามก่อนเสมอ** (พังแล้วย้อนยาก ไฟล์ซีน merge ไม่ได้) |
| อ่านข้อมูลจาก Unity (`get_scene_info` / `get_console_logs` / `get_gameobject`) | ไม่ต้อง |
| recompile หลังแก้โค้ด | ไม่ต้องถาม เจ้าของสลับไปเช็คใน Unity เองทุกรอบอยู่แล้ว |

**อย่าทำเองก่อนได้คำยืนยัน** — ถามแล้วต้องรอคำตอบจริงๆ ไม่ใช่ถามแล้วลงมือเลย

---

## 🚨 เรื่องที่เคยพังมาแล้ว อย่าให้ซ้ำ

1. **ไฟล์ untracked หายทั้งชุด** — เพื่อนใช้ GitHub Desktop pull/discard ทับ
   → เขียนไฟล์ใหม่เสร็จ **เตือนเจ้าของ commit ทันที** อย่าปล่อยค้าง
2. **โค้ดของเพื่อนห้ามรื้อ** — ถ้าต้องแตะ ให้แก้น้อยที่สุด แล้วบอกเจ้าของว่าแตะอะไรไปบ้าง
3. **`PlayerCombat.cs` เป็น God class ของเพื่อน** — เคยยัดโค้ดเข้าไปโดยไม่เตือน อย่าทำอีก

---

## 👥 ใครเป็นเจ้าของไฟล์ไหน

| git author | คือใคร | ดูรายละเอียด |
|---|---|---|
| `NANONC4` | **เจ้าของโปรเจกต์ (ผู้ใช้)** | แก้ได้อิสระ |
| `pongsatornthn-art` | **เพื่อนร่วมทีม** | แก้เท่าที่จำเป็น + แจ้งเสมอ |

กฎคร่าวๆ: **อะไรที่เกี่ยวกับตัวผู้เล่น = ของเพื่อนเกือบทั้งหมด**
ของผู้ใช้เอง = ระบบฟัน, ระบบเล็งปืน, ระบบสมุด/กระเป๋า

---

## 📚 เอกสาร — อ่านเฉพาะที่เกี่ยวกับงานรอบนั้น (อย่าอ่านหมด เปลืองโทเคน)

| ไฟล์ | อ่านเมื่อ |
|---|---|
| `HANDOFF.md` | **เริ่มงานใหม่ทุกครั้ง** — งานค้าง, ของที่พักไว้, log ล่าสุด |
| `PROJECT_CONTEXT.md` | ต้องรู้โครงสร้างรวม / FSM / ตารางปุ่ม / tech debt |
| `PLAYER_SYSTEMS.md` | งานแตะอะไรก็ตามที่เกี่ยวกับผู้เล่น |
| `INVENTORY_PLAN.md` | งานระบบสมุด/กระเป๋า/ไอเทม |
| `devlog.md` | เจ้าของเอาไปใส่สไลด์นำเสนอ — ภาษาง่ายๆ |
| `level_design_guide.md` | งานออกแบบด่าน |

**เอกสารพวกนี้ gitignore ไว้ (local only)**

---

## 🗣️ สื่อสารกับเจ้าของยังไง

- เป็น **นศ.ฝึกงาน** พูดภาษาพูด ไม่ถนัดศัพท์เทคนิค → **อย่าโยนศัพท์ให้ตัดสินใจ**
- อธิบายงานกว้างๆ มา → agent มีหน้าที่**แปลงเป็น spec เอง** แล้วยืนยันก่อนลงมือ
- ข้อมูลไม่พอ → ถามเป็นข้อๆ **พร้อมเสนอ default ที่แนะนำ**
- บอกวิธีตั้งค่าใน Unity → **ภาษาง่ายๆ เป็นขั้นตอน** ระบุชื่อช่องใน Inspector ตรงๆ
- ตอบภาษาไทย

---

## 🔌 MCP Unity

ต่อไว้แล้ว (`mcp-unity` by CoderGamester) — config: `d:\thesis\.mcp.json`
ต้องเปิด Unity + กด Start Server ที่ `Tools > MCP Unity > Server Window`

**ใช้ให้ประหยัดโทเคน:**
- ✅ คุ้ม: อ่าน Console, ใส่/ต่อ component, recompile
- ❌ ไม่คุ้ม: สร้าง UI เยอะๆ, `get_scenes_hierarchy` ทั้งซีน → **ให้เจ้าของทำเองใน Editor เร็วกว่า**
- อ่าน GameObject ใช้ `includeComponentProperties: false` ถ้าไม่จำเป็น

---

## 🌿 Branch

ปัจจุบัน: **`ทดสอบ2.0`** (แตกจาก `ทดสอบ` เมื่อ 2026-09-16 เพื่อกันเพื่อนอัปซีนทับ)
